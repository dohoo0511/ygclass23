# 데이터가 사라지지 않게 하는 설정 (한 번만 하면 됩니다)

## 왜 필요한가

저장소 열쇠(Master Key)가 `index.html` 안에 그대로 들어 있어서, **사이트 주소를 아는 누구나
학급 데이터를 읽고 덮어쓸 수 있습니다.** 예전 화면을 띄워 둔 휴대폰이 밤사이 기록을 통째로
되돌려 버린 것도 같은 이유입니다. 화면 쪽 코드에 아무리 방어를 넣어도, 그 코드를 쓰지 않는
기기에는 통하지 않습니다.

아래 설정을 마치면:

- 열쇠가 **서버에만** 있고 화면에는 없습니다. 예전 코드를 쓰는 기기는 **아예 저장하지 못합니다.**
- 저장은 **"내가 읽은 판번호가 아직 최신일 때만"** 서버가 받아 줍니다.
  오래된 화면이 덮어쓰는 일이 구조적으로 불가능해집니다.


## 저장소를 Cloudflare D1 으로 옮기기 (권장, 한 번만·10분)

지금까지는 바깥 서비스(jsonbin)에 자료를 맡겼습니다. **Cloudflare D1** 은 사이트가 이미 올라가 있는
곳(Cloudflare) 안의 데이터베이스라, 다음이 달라집니다.

| | jsonbin | Cloudflare D1 |
|---|---|---|
| 저장 방식 | 전체를 통째로 덮어씀 | 데이터베이스 한 줄 |
| 판번호 확인 | 서버가 **읽어 보고** 나서 씀 (아주 짧은 틈이 있음) | **한 문장으로 확인과 저장을 동시에** — 틈이 없음 |
| 용량 | 무료 100KB | 무료 5GB |
| 요청 수 | 무료 하루 10,000회 | 무료 하루 읽기 500만·쓰기 10만회 |
| 날짜별 사본 | 따로 KV 필요 | 같은 데이터베이스 안에 |
| 돈 | 늘리려면 유료 | 학급 규모에서는 계속 무료 |

### 1. 데이터베이스 만들기

Cloudflare → **Storage & Databases** → **D1 SQL Database** → **Create**
이름은 아무거나 (예: `ygclass23`)

### 2. 프로젝트에 연결

Workers & Pages → 이 프로젝트 → **설정** → **바인딩** → **D1 데이터베이스 추가**

| 항목 | 값 |
|---|---|
| 변수 이름 | `DB` ← **정확히 이 이름** |
| D1 데이터베이스 | 1번에서 만든 것 |

저장한 뒤 **재배포(Retry deployment)** 를 누르세요.

> 표(테이블)는 만들 필요가 없습니다. 서버 함수가 처음 실행될 때 알아서 만듭니다.

### 3. 자료 옮기기 — 할 일 없음

재배포 뒤 사이트를 한 번 열면, **비어 있는 D1 에 jsonbin 의 내용이 그대로 한 번 복사됩니다.**
그 뒤로는 D1 만 씁니다. jsonbin 은 더 이상 쓰지도, 바뀌지도 않습니다.

### 4. 확인

```
사이트주소/api/data?check=1
```

```json
{"ok":true,"storage":{"backend":"D1","d1":true,...},"rev":123,"users":34,...}
```

| 나온 내용 | 뜻 | 할 일 |
|---|---|---|
| `"backend":"D1"` | 옮겨졌습니다 | 끝입니다 |
| `"backend":"jsonbin"` | 바인딩이 아직 반영 안 됨 | 이름이 `DB` 가 맞는지 확인 후 **재배포** |
| `movedFromJsonbin` | 방금 옮겨왔다는 표시 | `users` 숫자가 학생 수와 맞는지만 확인 |

### 5. 정리 (며칠 지켜본 뒤)

며칠 써 보고 문제가 없으면 `JSONBIN_KEY` / `JSONBIN_BIN_ID` 를 지워도 됩니다.
**서두르지 마세요.** 남겨 두면 `restore.html` 의 '저장소 예전 버전' 만 계속 볼 수 있고,
그것 말고는 아무 영향이 없습니다.

> **D1 을 연결하지 않아도** 사이트는 예전처럼 jsonbin 으로 그대로 동작합니다.
> 배포 순서 때문에 수업이 멈추는 일은 없습니다.

## 예전 저장소(jsonbin) 설정 — 이미 끝난 부분

> D1 으로 옮겼다면 다시 할 일은 없습니다. 기록으로 남겨 둡니다.

### 1. 저장소 열쇠 새로 발급

[jsonbin.io](https://jsonbin.io) 로그인 → **API Keys** → Master Key **재발급(Regenerate)**

> 이 순간부터 예전 코드를 쓰는 모든 기기가 저장하지 못합니다. **데이터 파괴가 여기서 멈춥니다.**
> 새 열쇠는 다음 단계에서 쓰니 복사해 두세요.

### 2. Cloudflare에 열쇠 등록

Cloudflare → **Workers & Pages** → 이 프로젝트 → **설정** → **변수 및 비밀** 에서 두 개를 추가합니다.
(둘 다 `비밀(Secret)` 유형으로 넣으세요.)

| 이름 | 값 |
|---|---|
| `JSONBIN_KEY` | 1단계에서 발급받은 **새 Master Key** |
| `JSONBIN_BIN_ID` | `6a6dfa71da38895dfeaca6df` |

저장한 뒤 **재배포(Retry deployment)** 를 눌러야 반영됩니다.

### 3. 확인

휴대폰이나 PC에서 아래 주소를 엽니다. (`사이트주소` 부분만 바꾸세요)

```
사이트주소/api/data?check=1
```

**설정이 끝났으면** 이렇게 나옵니다. 학생 정보는 하나도 나오지 않습니다.

```json
{"ok":true,"step":"완료","message":"서버 함수가 저장소에 정상 연결되었습니다.","rev":123,"users":34,...}
```

| 나온 내용 | 뜻 | 할 일 |
|---|---|---|
| `"ok":true` | 정상 | 끝입니다 |
| `"step":"저장소 설정"` | D1 바인딩도 변수도 없음 | `DB` 바인딩을 등록하고 **재배포** |
| `"step":"저장소 연결"` | 열쇠나 Bin ID 가 틀림 | 새 Master Key 를 다시 확인 |
| 404 페이지 | 함수가 배포 안 됨 | Cloudflare 배포 완료 확인 |

### 4. 화면에서 열쇠 지우기 — **완료됨**

`index.html` 과 `restore.html` 에서 Master Key 를 모두 지웠습니다.
이제 두 파일 어디에도 열쇠가 없고, 저장소 주소도 들어 있지 않습니다.
열쇠는 서버 함수(`functions/api/data.js`)가 환경 변수로만 씁니다.

## 설정이 안 돼 있으면

예전에는 저장소에 직접 접속하는 방식으로 내려갔지만, 이제 그 길은 없습니다.
설정이 안 돼 있으면 사이트가 **조용히 예전 데이터를 쓰는 대신**, 무엇을 해야 하는지
알려주는 안내창을 띄우고 멈춥니다. 데이터가 망가지는 일은 없습니다.

```
⚠️ 저장소가 설정되지 않았어요.
   D1 바인딩(DB) 또는 JSONBIN_BIN_ID / JSONBIN_KEY 가 필요해요.
```

## 자동 백업

서버가 **하루 한 번 알아서** 데이터 사본을 남깁니다. 미리 해둘 일이 없고,
문제가 생기면 날짜를 골라 되돌리면 됩니다. 최근 **14일치**를 보관합니다.

**D1 을 연결했다면 따로 할 일이 없습니다.** 사본이 같은 데이터베이스 안에 남습니다.

<details>
<summary>D1 을 아직 안 쓸 때 — KV 로 백업 켜기</summary>

Cloudflare → **Storage & Databases** → **KV** → **Create a namespace** (이름은 아무거나)
→ Workers & Pages → 이 프로젝트 → **설정** → **바인딩** → **KV 네임스페이스 추가**

| 항목 | 값 |
|---|---|
| 변수 이름 | `BACKUPS` ← 정확히 이 이름 |
| KV 네임스페이스 | 방금 만든 것 |

저장한 뒤 **재배포**하세요. 나중에 D1 으로 옮겨도 **KV 에 남아 있던 사본은 그대로 목록에 나오고
되돌릴 수도 있습니다.**

</details>

### 확인

```
사이트주소/api/data?check=1
```

`backups` 항목을 보세요.

| 나온 내용 | 뜻 |
|---|---|
| `"available":true` | 켜졌습니다. 다음 저장 때 첫 사본이 남습니다 |
| `"where":"D1"` / `"KV"` | 사본이 어디에 남고 있는지 |
| `"available":false` | 아직 연결 안 됨 — `hint` 에 할 일이 적혀 있습니다 |

### 되돌리는 법

`사이트주소/restore.html` → **자동 백업 목록 보기** → 날짜 선택 → 관리자 또는 복구 비밀번호

> 설정하지 않아도 사이트는 그대로 동작합니다. 백업만 안 남습니다.

## 백업 파일 (restore.html) — 선택

자동 백업과 별개로, 학기 말처럼 중요한 시점의 사본을 파일로 따로 보관하고 싶을 때 씁니다.

```
사이트주소/restore.html
```

1. **지금 데이터 내려받기** — JSON 파일로 저장됩니다. **가끔(예: 주 1회, 큰 행사 전후) 눌러서 보관해두세요.**
2. **백업 파일로 되돌리기** — 보관해 둔 파일을 골라 그 시점으로 되돌립니다. 관리자 비밀번호가 필요합니다.
3. 저장소에 남은 예전 버전 — **jsonbin 을 쓸 때만** 동작합니다.
   D1 으로 옮긴 뒤에는 이 항목 대신 맨 위의 **자동 백업(날짜별 사본)** 을 쓰세요.

되돌리기는 서버가 비밀번호를 확인한 뒤에만 실행하고, 학생 정보가 없는 내용은 거부합니다.

## 복구 비밀번호

데이터가 예전 상태로 돌아갔을 때 쓰는 예비 열쇠입니다. 되돌리기에는 둘 중 아무거나 쓸 수 있습니다.

| 쓸 수 있는 비밀번호 | 어디에 있나 |
|---|---|
| 관리자 비밀번호 | 저장된 데이터 안 (`users.admin.password`) |
| **복구 비밀번호 `2323`** | 서버 함수 안에만 (화면 소스에는 없음) |

관리자 비밀번호가 기억나지 않거나, 학생이 알아냈을 때를 위한 것입니다.

**바꾸려면** Cloudflare → 변수 및 비밀에 `RECOVERY_PASSWORD` 를 등록하세요.
등록하면 그 값만 통하고, 기본값 `2323` 은 막힙니다.

> 복구 비밀번호로는 **되돌리기만** 할 수 있습니다. 로그인이나 평소 저장에는 쓸 수 없습니다.
